$(function(){
	$("#addButton").click(addBook);
	$(".book").click(showDescription);
});

var addBook = function(e){
	//when the addButton is clicked to add a book
	alert("The book has been added!");
};

var showDescription = function(e){
	//when a book is clicked!
}